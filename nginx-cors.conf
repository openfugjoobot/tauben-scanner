# Nginx Proxy Configuration for Tauben Scanner
# CORS is handled by Express backend (app.ts) - DO NOT add CORS headers here!

location / {
    # Proxy all requests to backend
    # Backend handles CORS via cors() middleware
    include conf.d/include/proxy.conf;
}